// Legacy entry point retained for backwards compatibility.
// The actual seeding logic now lives in scripts/seed-new-services.ts
// so that both Prisma and standalone TSX runners can share the same codebase.
import '../scripts/seed-new-services'
